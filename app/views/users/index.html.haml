%div.container
  %h1 Brukere, oversikt

  %table.table
    %thead
      %tr
        %th{scope: "col"} Brukernivå
        %th{scope: "col"} Epost
        %th
        %th
        %th

    %tbody
      - @users.each do |user|
        %tr
          %td
            - if user.admin?
              Administrator
            - else
              Kunde
          %td= user.email
          %td= link_to 'Vis', user, class: "text-primary"
          %td= link_to 'Endre', edit_user_path(user), class: "text-primary"
          %td= link_to 'Slett', user, method: :delete, data: { confirm: 'Er du sikker?' }, class: "text-primary"

  %br
