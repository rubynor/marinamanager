- provide(:title, "Add Berth")
- provide(:button_text, 'Add berth')
- provide(:form_action_url, berths_path)
- provide(:delete_action_disabled, 'disabled')

%h1 Add a berth
%div.row    
    %div.col-md-6.col-md-offset-3
        = render 'form' 
