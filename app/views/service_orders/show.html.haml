%div.container
  %p#notice= notice

  %p
    %b Bestiller:
    = link_to "#{@service_order.user.first_name} #{@service_order.user.last_name}", user_path(@service_order.user), class: "text-primary"
    %br

    %b Tjeneste:
    = @service_order.service.title
    %br

    %b Beskrivelse:
    = @service_order.service.description
    %br

    %b Pris:
    = @service_order.service.cost
    kr

    %br
    %b Bestilling opprettet:
    = @service_order.service.created_at.to_date

  = link_to 'Endre', edit_service_order_path(@service_order), class: "text-primary"
  \|
  = link_to 'Tilbake', service_orders_path, class: "text-primary"
